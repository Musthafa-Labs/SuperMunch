## Packages
framer-motion | Essential for comic-style pop animations, bouncy interactions, and page transitions
lucide-react | Standard icons needed for the UI elements

## Notes
The application is a single-page scrolling site. 
Menu items are fetched from `/api/menu`. 
Uses custom comic-style CSS implemented in `index.css`.
